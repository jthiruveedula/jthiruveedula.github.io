name: Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      rollback_ref:
        description: "Git ref to deploy (branch/tag/sha). Leave empty to deploy HEAD of master."
        required: false
        default: ""

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

env:
  NODE_VERSION: "22"
  KEEP_LAST_DEPLOYMENTS: 5

jobs:
  # Gate. Deploy previously ran on push to master with no dependency on any check, so a
  # commit that broke lint, types, the build, or the E2E suite still shipped to
  # production — observed in practice: a Deploy run went green while the CI run for the
  # same commit was still in progress. Gating on the CI workflow alone would not fix it,
  # because CI marks Lint and E2E `continue-on-error`, so a green CI does not mean the
  # tests passed. This job re-runs the checks with failures fatal, and deploy needs it.
  verify:
    name: Verify (lint, type-check, build, E2E)
    runs-on: ubuntu-latest
    outputs:
      deploy_sha: ${{ steps.deploy_vars.outputs.deploy_sha }}
      deploy_mode: ${{ steps.deploy_vars.outputs.deploy_mode }}
    steps:
      - name: Resolve deploy ref
        id: deploy_vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.rollback_ref }}" ]; then
            echo "deploy_sha=${{ inputs.rollback_ref }}" >> $GITHUB_OUTPUT
            echo "deploy_mode=rollback" >> $GITHUB_OUTPUT
          else
            echo "deploy_sha=${{ github.sha }}" >> $GITHUB_OUTPUT
            echo "deploy_mode=deploy" >> $GITHUB_OUTPUT
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.deploy_vars.outputs.deploy_sha }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --prefer-offline

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Install Playwright browser
        run: npx playwright install chromium

      - name: Run E2E tests
        run: npx playwright test --project=chromium --reporter=line
        timeout-minutes: 10

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: deploy-verify-failure-${{ github.run_id }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 14

  deploy:
    name: Build & deploy to GitHub Pages
    needs: [verify]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Carry resolved ref through from verify
        id: deploy_vars
        run: |
          echo "deploy_sha=${{ needs.verify.outputs.deploy_sha }}" >> $GITHUB_OUTPUT
          echo "deploy_mode=${{ needs.verify.outputs.deploy_mode }}" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.deploy_vars.outputs.deploy_sha }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Tag release
        if: steps.deploy_vars.outputs.deploy_mode == 'deploy'
        run: |
          git config --global user.email "deploy-bot@jthiruveedula.github.io"
          git config --global user.name "Deploy Bot"
          SHORT_SHA="${GITHUB_SHA::7}"
          if git rev-parse "v0.0.0-$SHORT_SHA" >/dev/null 2>&1; then
            echo "Tag v0.0.0-$SHORT_SHA already exists, skipping"
            exit 0
          fi
          git tag -a "v0.0.0-$SHORT_SHA" -m "Auto release $SHORT_SHA at $(date -u +%FT%TZ)"
          git push origin "v0.0.0-$SHORT_SHA"
          echo "Tagged release v0.0.0-$SHORT_SHA" >> $GITHUB_STEP_SUMMARY

      - name: Record deployment
        run: |
          DEPLOY_SHA="${{ steps.deploy_vars.outputs.deploy_sha }}"
          DEPLOY_MODE="${{ steps.deploy_vars.outputs.deploy_mode }}"
          DEPLOY_URL="${{ steps.deployment.outputs.page_url }}"
          DEPLOY_TIME=$(date -u +%FT%TZ)
          SHORT_SHA="${DEPLOY_SHA::7}"
          {
            echo "### 🚀 Deployment ($DEPLOY_MODE)"
            echo ""
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Mode | \`$DEPLOY_MODE\` |"
            echo "| SHA | \`$DEPLOY_SHA\` |"
            echo "| Short SHA | \`$SHORT_SHA\` |"
            echo "| URL | $DEPLOY_URL |"
            echo "| Time | \`$DEPLOY_TIME\` |"
            echo ""
            echo "### Rollback strategy"
            echo ""
            echo "GitHub Pages keeps the last \`${{ env.KEEP_LAST_DEPLOYMENTS }}\` deployments automatically per environment."
            echo ""
            echo "To roll back, use the Actions tab → select \`Deploy\` → \`Run workflow\` → set \`rollback_ref\` to:"
            echo ""
            echo "| Option | Value |"
            echo "|---|---|"
            echo "| Previous commit | \`<full-sha>\` (e.g. \`abc1234...\`) |"
            echo "| Previous branch | \`refs/heads/<branch-name>\` |"
            echo "| Release tag | \`v0.0.0-<short-sha>\` |"
            echo ""
            echo "Each successful master deploy creates an immutable tag \`v0.0.0-<sha>\` that can be redeployed."
          } >> $GITHUB_STEP_SUMMARY
